/**
 * 提交表格的公共混入方法
 * Created by 87487 on 2017/12/15.
 */
/*
import { mapState } from 'vuex'
export default {
    data() {
        return {
            title:'',      //当前窗口的标题
            index:'',      //当前编辑tr的index
        }
    },
    computed:{
        ...mapState({
            trDb:state => state.tableEdit.trDb,      //获取当前tr上面的数据
            trIdx:state => state.tableEdit.trIdx     //获取当前tr上面的index
        })
    },
    mounted() {
        this.title = this.trDb.name
        this.index = this.trIdx
    },
}
*/
